;; G1 demo script: top-level form runs when the script is loaded by Godot,
;; routing through our (godot-println ...) native into Godot's print pipeline.

(godot-println "hello from hello.tur, loaded by TurmericScript::_reload")
